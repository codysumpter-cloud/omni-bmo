# Copy to /etc/default/bmo-omni-agent (or source from user service)

# Omni token env used by config.json (omni_token_env)
PRISMBOT_API_TOKEN=

# Optional audio hints
# SDL_AUDIODRIVER=alsa
# PULSE_SERVER=unix:/run/user/1000/pulse/native
